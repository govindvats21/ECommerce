<div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-6">
              {paginatedItems.map((item) => (
                <div
                  key={item._id}
                  className="bg-white rounded-xl border border-gray-200 shadow-sm hover:shadow-lg transition transform hover:-translate-y-1 flex flex-col overflow-hidden cursor-pointer"
                  onClick={() => navigate(`/single-item/${item._id}`)}
                >
                  {/* Product Image */}
                  <div className="h-40 flex items-center justify-center bg-gray-50">
                    <img
                      src={item?.images[0]?.image1}
                      alt={item?.name}
                      className="h-full object-contain p-2 hover:scale-105 transition-transform"
                    />
                  </div>

                  {/* Product Info */}
                  <div className="p-4 flex flex-col flex-1 justify-start">
                    <p className="text-xs text-gray-500 mb-1">{item?.category}</p>
                    <h2 className="text-gray-800 font-semibold text-sm line-clamp-2 hover:text-green-600 cursor-pointer">
                      {item?.name}
                    </h2>
                    <div className="mt-auto">
                      <span className="block text-lg font-bold text-gray-900">
                        ₹{item?.price}
                      </span>
                      <button
                        className="mt-3 bg-green-600 hover:bg-green-700 text-white text-sm font-medium rounded-lg w-full py-2 flex items-center justify-center gap-2 transition transform hover:scale-105"
                        onClick={(e) => {
                          e.stopPropagation();
                          dispatch(
                            addToCart({
                              id: item._id,
                              name: item.name,
                              shop: item.shop,
                              price: item.price,
                              quantity: 1,
                              image: item?.images[0]?.image1,
                            })
                          );
                        }}
                      >
                        <CiShoppingCart size={18} />
                        Add to Cart
                      </button>
                    </div>
                  </div>
                </div>
              ))}
            </div>